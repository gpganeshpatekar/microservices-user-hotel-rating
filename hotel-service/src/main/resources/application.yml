server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/m-hotel-service
    username: root
    password: root
    dbcp2:
      driver-class-name: com.mysql.cj.jdbc.Driver
 
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect